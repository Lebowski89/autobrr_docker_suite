---

omegabrr_clients_lidarr_is_enabled: '{{ (omegabrr_clients_lidarr_api is defined) and
                                        (omegabrr_clients_lidarr_api is not none) and
                                        (omegabrr_clients_lidarr_api | trim | length > 0) }}'

omegabrr_clients_radarr_is_enabled: '{{ (omegabrr_clients_radarr_api is defined) and
                                        (omegabrr_clients_radarr_api is not none) and
                                        (omegabrr_clients_radarr_api | trim | length > 0) }}'

omegabrr_clients_radarr4k_is_enabled: '{{ (omegabrr_clients_radarr4k_api is defined) and
                                          (omegabrr_clients_radarr4k_api is not none) and
                                          (omegabrr_clients_radarr4k_api | trim | length > 0) }}'

omegabrr_clients_sonarr_is_enabled: '{{ (omegabrr_clients_sonarr_api is defined) and
                                        (omegabrr_clients_sonarr_api is not none) and
                                        (omegabrr_clients_sonarr_api | trim | length > 0) }}'

omegabrr_clients_sonarr4k_is_enabled: '{{ (omegabrr_clients_sonarr4k_api is defined) and
                                          (omegabrr_clients_sonarr4k_api is not none) and
                                          (omegabrr_clients_sonarr4k_api | trim | length > 0) }}'

omegabrr_clients_whisparr_is_enabled: '{{ (omegabrr_clients_whisparr_api is defined) and
                                          (omegabrr_clients_whisparr_api is not none) and
                                          (omegabrr_clients_whisparr_api | trim | length > 0) }}'